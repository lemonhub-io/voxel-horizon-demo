name = "voxel-horizon-mp"
main = "src/index.ts"
compatibility_date = "2025-04-07"
compatibility_flags = ["nodejs_compat"]

[dev]
port = 8787

[[durable_objects.bindings]]
name = "PLANET_ROOM"
class_name = "PlanetRoom"

[[migrations]]
tag = "v1"
# Free plan requires SQLite-backed DO classes. Session state stays in-memory;
# we still do not host player/world saves on the server.
new_sqlite_classes = ["PlanetRoom"]
